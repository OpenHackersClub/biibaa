# Workspace temp
.tmp/
.claude/worktrees/

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Build
dist/
build/

# Runtime data — raw payloads, warehouse, fan-out cache (per SPEC §8).
# data/briefs/ IS committed: site/src/content.config.ts reads them at Astro
# build time on the Cloudflare Pages runner, which has no access to your
# local data/.
data/raw/
data/warehouse.duckdb
data/dependents_cache.sqlite

# Editor
.vscode/
.idea/
.DS_Store

# Local env (copy from env.sample)
.env
.env.local
